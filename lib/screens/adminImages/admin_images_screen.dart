import 'package:flutter/material.dart';

class AdminImagesScreen extends StatelessWidget {
  const AdminImagesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Container(
        child: Center(
          child: Text('Hacer admin de imagenes'),
        ),
      ),
    );
  }
}
